# State directory

This directory stores operational artifacts generated by the pipeline at runtime.

Tracked files:

- `current_scorecard.toml`
- `candidate_scorecard.toml`
- `results_ledger.jsonl`

Ignored generated subdirectories:

- `state/psl_indices/`
- `state/objective_forecast/`

These ignored paths are re-created by the CLI as needed.
